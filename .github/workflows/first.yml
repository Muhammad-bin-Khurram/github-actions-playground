name: My first workflow

on: 
  push:
    branches:
      - main

jobs: 
  say-hello:
    runs-on: ubuntu-latest

    steps:

      - name: Print Hello 
        run: echo "Hello Github Actions"

      - name: Print Current Date
        run: Date
      
      - name: show directory
        run: ls
